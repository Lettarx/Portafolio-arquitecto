<header class="hero">
      <div class="hero-overlay"></div>
      <div class="topbar container">
        <div class="brand">
          <img src="/logo.png" alt="Logo" class="logo" />
          <span class="brand-text">Estudio Arquitecto</span>
        </div>
      </div>
      <div class="hero-content container">
        <h1>Estudio Martin Valdés</h1>
        <h2>Arquitectura Contemporánea</h2>
        <p class="lead">
          Diseño arquitectónico contemporáneo con un enfoque innovador y
          elegante.<br />
          Creamos espacios únicos que inspiran.
        </p>
        <a href="#portafolio" class="btn btn-primary">Ver Proyectos</a>
      </div>
</header>

<style>
    .hero {
  position: relative;
  min-height: 88vh;
  background: url("/headerfondo.png") center/cover no-repeat;
  color: #fff;
}

.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    145deg,
    rgba(34, 34, 34, 0.6),
    rgba(0, 0, 0, 0.3)
  );
}

.hero .container {
  position: relative;
  z-index: 2;
}

.hero-content {
  padding: 14vh 0 12vh;
}

.hero h1 {
  font-size: clamp(40px, 7vw, 72px);
  font-weight: 700;
  color: #fff;
  letter-spacing: 0.2px;
}
.hero h2 {
  font-size: clamp(18px, 2.6vw, 28px);
  font-weight: 300;
  color: #e9e9e9;
  margin-bottom: 8px;
}
.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 0;
  position: relative;
  z-index: 2;
}

.brand {
  display: flex;
  align-items: center;
  gap: 10px;
}
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 16px;
}
.brand-text {
  font-weight: 600;
  color: #fff;
}
.lead {
  font-size: clamp(14px, 1.6vw, 18px);
  color: #e0e0e0;
  margin: 12px 0 24px;
}
.btn {
  display: inline-block;
  padding: 12px 20px;
  border-radius: 999px;
  font-weight: 600;
  text-decoration: none;
}

.btn-primary {
  background: var(--accent);
  color: #222;
  border: 2px solid var(--accent);
}
.logo {
  width: 64px;
  height: 64px;
  object-fit: cover;
  border-radius: 6px;
}
</style>